import { NgModule } from '@angular/core';
import { MessageBusComponent } from './message-bus.component';



@NgModule({
  declarations: [MessageBusComponent],
  imports: [
  ],
  exports: [MessageBusComponent]
})
export class MessageBusModule { }
